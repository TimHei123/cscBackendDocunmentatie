server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com;  # REPLACE dynamically in setup.sh

    # Redirect HTTP to HTTPS except for certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# SSL server block — commented initially for certbot first run
# Uncomment this after certbot generates certs

# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name your-domain.com;
#
#     ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     location / {
#         root /usr/share/nginx/html;
#         index index.html api.html;
#         try_files $uri $uri/ =404;
#     }
#
#     location /swagger/ {
#         proxy_pass http://swagger-ui:8080/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
