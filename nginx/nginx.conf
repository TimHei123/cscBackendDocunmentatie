worker_processes 1;

# events {
#     worker_connections 1024;
# }

# http {
#     include       /etc/nginx/mime.types;
#     default_type  application/octet-stream;

#     upstream swaggerui {
#         server swagger-ui:8080;
#     }

#     server {
#         listen 80;
#         server_name timonheidenreich.nl;

#         location /.well-known/acme-challenge/ {
#             root /var/www/certbot;
#             try_files $uri =404;
#         }

#         # Redirect all HTTP traffic to HTTPS
#         location / {
#             return 301 https://$host$request_uri;
#         }
#     }

#     server {
#         listen 443 ssl;
#         server_name timonheidenreich.nl;

#         ssl_certificate /etc/letsencrypt/live/timonheidenreich.nl/fullchain.pem;
#         ssl_certificate_key /etc/letsencrypt/live/timonheidenreich.nl/privkey.pem;
#         ssl_protocols TLSv1.2 TLSv1.3;
#         ssl_ciphers HIGH:!aNULL:!MD5;

#         # Proxy requests under /csc-self-servics/api-docs/ to Swagger UI
#         location /csc-self-servics/api-docs/ {
#             # Rewrite the path so that /csc-self-servics/api-docs/swagger.yaml becomes /swagger.yaml
#             rewrite ^/csc-self-servics/api-docs/(.*)$ /$1 break;
#             proxy_pass http://swaggerui/;
#             proxy_http_version 1.1;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#             proxy_set_header X-Forwarded-Proto $scheme;
#             add_header 'Access-Control-Allow-Origin' '*' always;
#         }

#         # Redirect any other requests to the API docs path
#         location / {
#             return 301 /csc-self-servics/api-docs/;
#         }
#     }
# }


events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    keepalive_timeout  65;
    gzip  on;

    upstream swagger_backend {
        server swagger-ui:8080;
    }

    server {
        listen 80;
        server_name timonheidenreich.nl;

        location / {
            proxy_pass http://swagger_backend;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # For certbot HTTP validation challenges (if needed)
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }
    }
}
